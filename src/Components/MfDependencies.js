import React from "react";

const MfDependencies = () => {
  /*
    TODO:
    0. find a npm package/library to visualize said vertex graph.
    1. visualize a vertex graph of MF dependencies.
    2. allow a user to query about a specific ancestor sub graph and visualize it as well
        2.1. input fields
        2.2. color the relevent vertex in a different color.
    --------------
    3. allow the user to enter multiple names simultaneously
    4. genereate a compilation job to inject to the MF.
     */
  let nodes = [];
  let dependenciesEdges = [];
  return <div> Python MF Dependencies Works!</div>;
};

export default MfDependencies;
